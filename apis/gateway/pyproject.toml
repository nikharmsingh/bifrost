[project]
name = "gateway"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "core",
    "llm",
]

[tool.uv.sources]
core = { workspace = true }
llm  = { workspace = true }

[tool.uv]
package = false
